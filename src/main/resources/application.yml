server:
  servlet:
    encoding:
      force-response: true
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
#    url: jdbc:mariadb://127.0.0.1:3307
    username: root
    password: dlqns6312!

jwt:
  token:
    secret: fast-campus.simple_sns_2022_secret_key
